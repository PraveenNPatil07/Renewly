[pytest]
addopts = -m "not integration"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests as hitting the real database and external APIs (deselect with '-m "not integration"')
